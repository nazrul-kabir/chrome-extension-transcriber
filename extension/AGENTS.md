# Agent Instructions for Chrome Extension

This directory contains the source code for the Chrome Extension.

## Building the Extension

To build the extension, run the following command from the root of the repository:

```bash
npm run build
```

This will generate the necessary files in the `dist/extension` directory. You can then load this directory as an unpacked extension in Chrome.
